import React from "react";
import "./LeftBox.css";

const LeftBox = () => {
  return (
    <div className="left-box">
      <div className="left-box-text">
        This is some random text that is styled according to the provided
        guidelines.
        <p>
          {" "}
          Here are the official instructions: empty (but it should be responsive
          for laptop, not mobile) focus on the two widgets on the right hand
          side the first widget has three buttons, the second widget has a list
          of items. The first widget frafdafsadf
        </p>
      </div>
    </div>
  );
};

export default LeftBox;
